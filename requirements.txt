# openssl_encrypt runtime dependencies — single source of truth (gitlab#301).
#
# This file is a pointer: the actual pins live in the hash-pinned lockfile
# requirements-prod.txt (compiled from requirements-prod.in; regenerate with
# scripts/update_dependencies.sh). pip enforces hash-checking automatically
# through this include, so `pip install -r requirements.txt` is a fully
# hash-verified install.
#
# Development tooling (pytest, black, pylint, ...): requirements-dev.txt.
# HSM extras: requirements-hsm.txt.
-r requirements-prod.txt
