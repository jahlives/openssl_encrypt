# Production dependencies for openssl_encrypt
# Core dependencies
cryptography>=50.0.0,<51.0.0  # Dependabot: CVE-2026-69247 (PKCS#7 EnvelopedData Bleichenbacher oracle); not exercised by this project (uses AEAD/KEM, not CMS), bumped as hygiene
argon2-cffi>=23.1.0,<24.0.0
PyYAML>=6.0.2,<7.0.0
blake3>=1.0.0,<2.0.0
jsonschema>=4.0.0,<5.0.0
requests>=2.31.0,<3.0.0

# QR Code Key Distribution dependencies (v1.3.0)
Pillow>=12.3.0,<13.0.0  # Dependabot: 13 image-parsing CVEs fixed in 12.3.0 (Pillow parses QR/portable-media images)
qrcode[pil]>=7.0.0,<9.0.0
pyzbar>=0.1.9,<1.0.0

# Windows-specific dependencies
pywin32>=306,<307; sys_platform == 'win32'

# Optional post-quantum cryptography dependencies (commented by default)
# liboqs-python>=0.7.0

# Optional RandomX KDF dependencies (aarch64 uses the fixed fork,
# gitlab#282: PyPI binding never links the ARM JIT assembly)
RandomX>=1.1.10 ; platform_machine != "aarch64"
RandomX @ git+https://github.com/jahlives/RandomX-Python@101d3e6826dcf27e0253a44a6b7926b9033b9666 ; platform_machine == "aarch64"
